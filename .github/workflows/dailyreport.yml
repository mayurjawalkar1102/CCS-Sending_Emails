name: daily-report

on:
  push:
    branches: 
      - main  
  
  workflow_dispatch:


jobs:
  my-jobs: 
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "Hello, World!"